<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:f="clr-namespace:Fonts"
    xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
    xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.TextInputLayout;assembly=Syncfusion.Maui.Toolkit"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit">

    <x:Double x:Key="sizeNone">0</x:Double>
    <x:Double x:Key="size20">2</x:Double>
    <x:Double x:Key="size40">4</x:Double>
    <x:Double x:Key="size60">6</x:Double>
    <x:Double x:Key="size80">8</x:Double>
    <x:Double x:Key="size100">10</x:Double>
    <x:Double x:Key="size120">12</x:Double>
    <x:Double x:Key="size160">16</x:Double>
    <x:Double x:Key="size200">20</x:Double>
    <x:Double x:Key="size240">24</x:Double>
    <x:Double x:Key="size280">28</x:Double>
    <x:Double x:Key="size320">32</x:Double>
    <x:Double x:Key="size360">36</x:Double>
    <x:Double x:Key="size400">40</x:Double>
    <x:Double x:Key="size480">48</x:Double>
    <x:Double x:Key="size520">52</x:Double>
    <x:Double x:Key="size560">56</x:Double>

    <!-- https://fluent2.microsoft.design/typography/ -->
    <OnIdiom x:Key="IconSize"
             x:TypeArguments="x:Double"
             Default="20">
        <OnIdiom.Desktop>32</OnIdiom.Desktop>
    </OnIdiom>
    
    <OnIdiom x:Key="IconSizeSmall"
             x:TypeArguments="x:Double"
             Default="12">
        <OnIdiom.Desktop>18</OnIdiom.Desktop>
    </OnIdiom>

    <OnIdiom x:Key="LayoutPadding"
             x:TypeArguments="Thickness"
             Default="15">
        <OnIdiom.Desktop>30</OnIdiom.Desktop>
    </OnIdiom>

    <OnIdiom x:Key="LayoutSpacing"
             x:TypeArguments="x:Double"
             Default="5">
        <OnIdiom.Desktop>15</OnIdiom.Desktop>
    </OnIdiom>

    <FontImageSource x:Key="IconDashboard"
                     Glyph="{x:Static f:FluentUI.diagram_24_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{toolkit:AppThemeResource Icon}"
                     Size="{StaticResource IconSize}" />

    <FontImageSource x:Key="IconProjects"
                     Glyph="{x:Static f:FluentUI.list_24_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{toolkit:AppThemeResource Icon}"
                     Size="{StaticResource IconSize}" />

    <FontImageSource x:Key="IconMeta"
                     Glyph="{x:Static f:FluentUI.info_24_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{toolkit:AppThemeResource Icon}"
                     Size="{StaticResource IconSize}" />

    <FontImageSource x:Key="IconRibbon"
                     Glyph="{x:Static f:FluentUI.ribbon_20_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{toolkit:AppThemeResource Icon}"
                     Size="{StaticResource IconSize}" />

    <FontImageSource x:Key="IconAdd"
                Glyph="{x:Static f:FluentUI.add_32_regular}"
                FontFamily="{x:Static f:FluentUI.FontFamily}"
                Color="{toolkit:AppThemeResource Icon}"
                Size="{StaticResource IconSize}" />

    <FontImageSource x:Key="IconDelete"
                Glyph="{x:Static f:FluentUI.delete_32_regular}"
                FontFamily="{x:Static f:FluentUI.FontFamily}"
                Color="{toolkit:AppThemeResource Icon}"
                Size="{StaticResource IconSize}" />

        <FontImageSource x:Key="IconClean"
                Glyph="{x:Static f:FluentUI.broom_32_regular}"
                FontFamily="{x:Static f:FluentUI.FontFamily}"
                Color="{toolkit:AppThemeResource Icon}"
                Size="{StaticResource size240}" />

        <FontImageSource x:Key="IconLight"
                Glyph="{x:Static f:FluentUI.weather_sunny_28_regular}"
                FontFamily="{x:Static f:FluentUI.FontFamily}"
                Color="{toolkit:AppThemeResource Icon}"
                Size="{StaticResource size240}" />
        <FontImageSource x:Key="IconDark"
                Glyph="{x:Static f:FluentUI.weather_moon_28_regular}"
                FontFamily="{x:Static f:FluentUI.FontFamily}"
                Color="{toolkit:AppThemeResource Icon}"
                Size="{StaticResource size240}" />

    <Style TargetType="Label">
        <Setter Property="TextColor"
                Value="{toolkit:AppThemeResource PrimaryText}" />
        <Setter Property="BackgroundColor"
                Value="Transparent" />
        <Setter Property="FontSize"
                Value="17" />
        <Setter Property="LineHeight"
                Value="1.29" />
        <!-- 22 -->
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor"
                                    Value="{toolkit:AppThemeResource PrimaryText}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
    <Style x:Key="Caption2"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="12" />
        <Setter Property="LineHeight"
                Value="1.33" />
        <!-- 16 -->
    </Style>
    <Style x:Key="Caption1"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="10" />
        <!-- <Setter Property="LineHeight"
                Value="1" /> -->
        <!-- 18 -->
    </Style>
    <Style x:Key="Caption1Strong"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="13" />
        <Setter Property="LineHeight"
                Value="1.38" />
        <!-- 18 -->
        <Setter Property="FontFamily"
                Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}" />
        <Setter Property="FontAttributes"
                Value="{OnPlatform Default=None, Android=Bold}" />
    </Style>
    <Style x:Key="Body2"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="15" />
        <Setter Property="LineHeight"
                Value="1.33" />
        <!-- 20 -->
    </Style>
    <Style x:Key="Body2Strong"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="15" />
        <Setter Property="LineHeight"
                Value="1.33" />
        <!-- 20 -->
        <Setter Property="FontFamily"
                Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}" />
        <Setter Property="FontAttributes"
                Value="{OnPlatform Default=None, Android=Bold}" />
    </Style>
    <Style x:Key="Body1"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="17" />
        <Setter Property="LineHeight"
                Value="1.29" />
        <!-- 22 -->
    </Style>
    <Style x:Key="Body1Strong"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="17" />
        <Setter Property="LineHeight"
                Value="1.29" />
        <!-- 22 -->
        <Setter Property="FontFamily"
                Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}" />
        <Setter Property="FontAttributes"
                Value="{OnPlatform Default=None, Android=Bold}" />
    </Style>
    <Style x:Key="Title3"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="20" />
        <Setter Property="LineHeight"
                Value="1.25" />
        <!-- 25 -->
        <Setter Property="FontFamily"
                Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}" />
        <Setter Property="FontAttributes"
                Value="{OnPlatform Default=None, Android=Bold}" />
    </Style>
    <Style x:Key="Title2"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="22" />
        <Setter Property="LineHeight"
                Value="1.27" />
        <!-- 28 -->
        <Setter Property="FontFamily"
                Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}" />
        <Setter Property="FontAttributes"
                Value="{OnPlatform Android=Bold, Default=None}" />
    </Style>
    <Style x:Key="Title1"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="28" />
        <Setter Property="LineHeight"
                Value="1.21" />
        <!-- 34 -->
        <Setter Property="FontAttributes"
                Value="{OnPlatform WinUI=None, Default=Bold}" />
    </Style>
    <Style x:Key="LargeTitle"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="34" />
        <Setter Property="LineHeight"
                Value="1.21" />
        <!-- 41 -->
        <Setter Property="FontAttributes"
                Value="{OnPlatform WinUI=None, Default=Bold}" />
    </Style>
    <Style x:Key="Display"
           TargetType="Label">
        <Setter Property="FontSize"
                Value="60" />
        <Setter Property="LineHeight"
                Value="1.17" />
        <!-- 70 -->
        <Setter Property="FontAttributes"
                Value="{OnPlatform WinUI=None, Default=Bold}" />
    </Style>

    <Style TargetType="Border">
        <Setter Property="StrokeShape" Value="RoundRectangle 20"/>
        <Setter Property="StrokeThickness" Value="0"/>
        <Setter Property="Padding" Value="{OnIdiom 15, Desktop=20}"/>
    </Style>

    <Style TargetType="Border" x:Key="CardStyle">
        <Setter Property="StrokeShape" Value="RoundRectangle 20"/>
        <Setter Property="Background" Value="{toolkit:AppThemeResource SecondaryBackground}"/>
        <Setter Property="StrokeThickness" Value="0"/>
        <Setter Property="Padding" Value="{OnIdiom 15, Desktop=20}"/>
    </Style>
    <Style TargetType="Border" x:Key="FiltersStyle">
        <Setter Property="StrokeShape" Value="RoundRectangle 0,0,10,10"/>
        <Setter Property="Background" Value="{toolkit:AppThemeResource SecondaryBackground}"/>
        <Setter Property="StrokeThickness" Value="0"/>
        <Setter Property="Padding" Value="{OnIdiom 15, Desktop=20}"/>
    </Style>

    <!-- ========================================= -->
    <!-- TEXT INPUT LAYOUT                         -->
    <!-- ========================================= -->
    <sf:LabelStyle x:Key="lightThemeLabelStyle" TextColor="{toolkit:AppThemeResource SecondaryText}" FontSize="{OnIdiom 13, Desktop=16}"/>
    <sf:LabelStyle x:Key="darkThemeLabelStyle" TextColor="{toolkit:AppThemeResource SecondaryText}" FontSize="{OnIdiom 13, Desktop=16}"/>
    <sf:LabelStyle x:Key="errorLabelStyleLight" TextColor="{toolkit:AppThemeResource Error}" FontSize="12"/>
    <sf:LabelStyle x:Key="errorLabelStyleDark" TextColor="{toolkit:AppThemeResource Error}" FontSize="12"/>

    <Style TargetType="sf:SfTextInputLayout">
        <Setter Property="ContainerType" Value="Outlined"/>
        <Setter Property="ContainerBackground" Value="Transparent"/>
        <Setter Property="OutlineCornerRadius" Value="12"/>
        <Setter Property="Stroke" Value="{toolkit:AppThemeResource  Accent1}"/>
        <Setter Property="FocusedStrokeThickness" Value="2"/>
        <Setter Property="UnfocusedStrokeThickness" Value="1"/>
        <Setter Property="ReserveSpaceForAssistiveLabels" Value="False"/>
        <Setter Property="HintLabelStyle" Value="{StaticResource lightThemeLabelStyle}"/>
        <Setter Property="ErrorLabelStyle" Value="{StaticResource errorLabelStyleLight}"/>
    </Style>

    <!-- Leading icon inside SfTextInputLayout -->
    <Style x:Key="InputLeadingIcon" TargetType="Label">
        <Setter Property="FontFamily" Value="{x:Static f:FluentUI.FontFamily}"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource PrimaryText}"/>
        <Setter Property="VerticalOptions" Value="Center"/>
    </Style>

    <!-- Error banner container -->
    <Style x:Key="ErrorBanner" TargetType="Border">
        <Setter Property="StrokeShape" Value="RoundRectangle 12"/>
        <Setter Property="BackgroundColor" Value="{toolkit:AppThemeResource Primary}"/>
        <Setter Property="StrokeThickness" Value="0"/>
        <Setter Property="Padding" Value="14,10"/>
    </Style>

    <!-- Error banner icon -->
    <Style x:Key="ErrorBannerIcon" TargetType="Label">
        <Setter Property="Text" Value="{x:Static f:FluentUI.error_circle_20_regular}"/>
        <Setter Property="FontFamily" Value="{x:Static f:FluentUI.FontFamily}"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource Error}"/>
        <Setter Property="VerticalOptions" Value="Center"/>
    </Style>

    <!-- Error banner text -->
    <Style x:Key="ErrorBannerText" TargetType="Label">
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource Error}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="LineHeight" Value="1.38"/>
        <Setter Property="VerticalOptions" Value="Center"/>
        <Setter Property="LineBreakMode" Value="WordWrap"/>
        <Setter Property="HorizontalOptions" Value="FillAndExpand"/>
    </Style>

    <!-- Circular icon button (e.g. footer icons) -->
    <Style x:Key="CircleIconButton" TargetType="Border">
        <Setter Property="StrokeShape" Value="RoundRectangle 22"/>
        <Setter Property="HeightRequest" Value="38"/>
        <Setter Property="WidthRequest" Value="38"/>
        <Setter Property="StrokeThickness" Value="1"/>
        <Setter Property="Stroke" Value="{toolkit:AppThemeResource Accent1}"/>
        <Setter Property="BackgroundColor" Value="Transparent"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <!-- Icon inside CircleIconButton -->
    <Style x:Key="CircleIconLabel" TargetType="Label">
        <Setter Property="FontFamily" Value="{x:Static f:FluentUI.FontFamily}"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource SecondaryText}"/>
        <Setter Property="HorizontalOptions" Value="Center"/>
        <Setter Property="VerticalOptions" Value="Center"/>
    </Style>

    <!-- ========================================= -->
    <!-- BUTTON VARIANTS                           -->
    <!-- ========================================= -->

    <!-- Primary Button: Filled, rounded pill shape -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="BackgroundColor" Value="{toolkit:AppThemeResource Primary}"/>
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource PrimaryText}"/>
        <Setter Property="FontFamily" Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}"/>
        <Setter Property="FontAttributes" Value="{OnPlatform Default=None, Android=Bold}"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="HeightRequest" Value="52"/>
        <Setter Property="Padding" Value="10,0"/>
        <Setter Property="BorderWidth" Value="0"/>
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal"/>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{toolkit:AppThemeResource Disabled}"/>
                            <Setter Property="TextColor" Value="{toolkit:AppThemeResource SecondaryText}"/>
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver"/>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <!-- Secondary Button: Outlined, transparent background -->
    <Style x:Key="SecondaryButton" TargetType="Button">
        <Setter Property="BackgroundColor" Value="Transparent"/>
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource PrimaryText}"/>
        <Setter Property="FontFamily" Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}"/>
        <Setter Property="FontAttributes" Value="{OnPlatform Default=None, Android=Bold}"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="HeightRequest" Value="52"/>
        <Setter Property="Padding" Value="24,0"/>
        <Setter Property="BorderWidth" Value="1.5"/>
        <Setter Property="BorderColor" Value="{toolkit:AppThemeResource Accent1}"/>
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal"/>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="{toolkit:AppThemeResource SecondaryText}"/>
                            <Setter Property="BorderColor" Value="{toolkit:AppThemeResource Disabled}"/>
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="PointerOver"/>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <!-- Danger Button: For destructive actions -->
    <Style x:Key="DangerButton" TargetType="Button">
        <Setter Property="BackgroundColor" Value="Transparent"/>
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource Error}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="CornerRadius" Value="26"/>
        <Setter Property="HeightRequest" Value="48"/>
        <Setter Property="Padding" Value="20,0"/>
        <Setter Property="BorderWidth" Value="1.5"/>
        <Setter Property="BorderColor" Value="{toolkit:AppThemeResource Error}"/>
    </Style>
        <!-- Danger Button: For destructive actions -->
    <Style x:Key="CleanLogsButton" TargetType="Button">
        <Setter Property="BackgroundColor" Value="Transparent"/>
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource Error}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="HeightRequest" Value="10"/>
        <Setter Property="Padding" Value="1,0"/>
        <Setter Property="BorderWidth" Value="1.5"/>
        <Setter Property="BorderColor" Value="{toolkit:AppThemeResource Error}"/>
    </Style>

    <!-- Ghost/Text Button: No background, no border -->
    <Style x:Key="GhostButton" TargetType="Button">
        <Setter Property="BackgroundColor" Value="Transparent"/>
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource PrimaryText}"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="BorderWidth" Value="0"/>
        <Setter Property="Padding" Value="12,8"/>
    </Style>

    <!-- ========================================= -->
    <!-- ERROR MESSAGE                             -->
    <!-- ========================================= -->
    <Style x:Key="ErrorMessage" TargetType="Label">
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource Error}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="LineHeight" Value="1.38"/>
        <Setter Property="HorizontalTextAlignment" Value="Start"/>
        <Setter Property="Padding" Value="12,8"/>
    </Style>

    <!-- ========================================= -->
    <!-- LINK LABEL (tappable text)                -->
    <!-- ========================================= -->
    <Style x:Key="LinkLabel" TargetType="Label">
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource PrimaryText}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontFamily" Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}"/>
        <Setter Property="FontAttributes" Value="{OnPlatform Default=None, Android=Bold}"/>
    </Style>

    <!-- ========================================= -->
    <!-- SECTION HEADER                            -->
    <!-- ========================================= -->
    <Style x:Key="SectionHeader" TargetType="Label">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="TextTransform" Value="Uppercase"/>
        <Setter Property="CharacterSpacing" Value="1.5"/>
        <Setter Property="TextColor" Value="{toolkit:AppThemeResource SecondaryText}"/>
        <Setter Property="FontFamily" Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}"/>
        <Setter Property="FontAttributes" Value="{OnPlatform Default=None, Android=Bold}"/>
        <Setter Property="Margin" Value="0,8,0,4"/>
    </Style>

    <!-- ========================================= -->
    <!-- ELEVATED CARD (with shadow)               -->
    <!-- ========================================= -->
    <Style x:Key="ElevatedCard" TargetType="Border">
        <Setter Property="StrokeShape" Value="RoundRectangle 16"/>
        <Setter Property="Background" Value="{toolkit:AppThemeResource SecondaryBackground}"/>
        <Setter Property="StrokeThickness" Value="0"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Shadow">
            <Shadow Brush="{toolkit:AppThemeResource ShadowSecondary}"
                    Offset="0,2"
                    Radius="8"
                    Opacity="1"/>
        </Setter>
    </Style>

    <!-- ========================================= -->
    <!-- FORM CARD (for grouping form fields)      -->
    <!-- ========================================= -->
    <Style x:Key="FormCard" TargetType="Border">
        <Setter Property="StrokeShape" Value="RoundRectangle 20"/>
        <Setter Property="Background" Value="{toolkit:AppThemeResource SecondaryBackground}"/>
        <Setter Property="StrokeThickness" Value="0"/>
        <Setter Property="Padding" Value="20,16"/>
    </Style>

    <!-- ========================================= -->
    <!-- DIVIDER                                   -->
    <!-- ========================================= -->
    <Style x:Key="Divider" TargetType="BoxView">
        <Setter Property="HeightRequest" Value="1"/>
        <Setter Property="Color" Value="{toolkit:AppThemeResource Accent1}"/>
        <Setter Property="HorizontalOptions" Value="Fill"/>
        <Setter Property="Margin" Value="0,4"/>
    </Style>

    <!-- ========================================= -->
    <!-- SHIMMER / PULL TO REFRESH                 -->
    <!-- ========================================= -->
    <Style TargetType="BoxView" x:Key="ShimmerCustomViewStyle">
        <Setter Property="BackgroundColor" Value="Gray" />
        <Setter Property="HorizontalOptions" Value="Fill" />
        <Setter Property="VerticalOptions" Value="Center" />
    </Style>

    <Style TargetType="pullToRefresh:SfPullToRefresh">
        <Setter Property="TransitionMode" Value="SlideOnTop"/>
        <Setter Property="PullingThreshold" Value="100"/>
        <Setter Property="ProgressColor" Value="{toolkit:AppThemeResource Primary}"/>
    </Style>

</ResourceDictionary>