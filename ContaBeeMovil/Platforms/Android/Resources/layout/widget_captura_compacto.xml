<?xml version="1.0" encoding="utf-8"?>
<!-- Widget compacto (1×1): logo con badge de cámara y etiqueta, como ícono de app.
     La etiqueta va en blanco con sombra (patrón de los labels del launcher). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center">

    <FrameLayout
        android:layout_width="54dp"
        android:layout_height="54dp">

        <ImageView
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:src="@drawable/logo512"
            android:scaleType="fitCenter"
            android:contentDescription="ContaBee" />

        <FrameLayout
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_gravity="bottom|end"
            android:background="@drawable/widget_badge">

            <ImageView
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_widget_camera"
                android:scaleType="fitCenter" />
        </FrameLayout>
    </FrameLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Capturar"
        android:textSize="11sp"
        android:textColor="#FFFFFF"
        android:shadowColor="#99000000"
        android:shadowDx="0"
        android:shadowDy="1"
        android:shadowRadius="3"
        android:singleLine="true" />

</LinearLayout>
